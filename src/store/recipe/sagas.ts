import {takeLatest} from 'redux-saga/effects';
import {recipeActions} from 'src/store/recipe/slice';

export function* watchRecipe() {
  // yield takeLatest(recipeActions.someAction, handleSomeAction)
}
