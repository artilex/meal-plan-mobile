import {StyleSheet} from 'react-native';
import {COLOR, ICON_SIZE} from 'src/constants/theme';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: COLOR.GREEN1,
    borderRadius: ICON_SIZE.EXTRA_LARGE,
  },
});
